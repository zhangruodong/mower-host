# 安全须知与免责声明

本工程是针对**我们这一台割草机**的特定硬件（串口设备名、雷达安装位置与角度、`ekf.yaml`
的标定系数、里程计修正系数等均按实车调）写的控制思路参考实现，不是通用的产品软件。
换一台车、换一套接线、把雷达挪个位置，相关参数都得**重新标定**。
本工程控制的是真实的机电设备，上机前请先把这份文档读完。

---

## 一、这不是产品

没有做过任何安全认证（CE / CCC / ISO 12100 / ISO 13849 一个都没有），没有外壳防护等级，
没有 EMC 测试，没有失效模式分析。它是一个人自己攒出来、能跑起来的工程实现。

**别把它用在有人走动的公共区域，别把它当成商品卖给或交给别人用。**

## 二、刀片

**上位机不发任何刀片指令。出于安全，刀片控制那一整套已经删掉了**——固件里这条命令没有
分支，上位机确认不了刀片转没转、也保证不了叫得停，这种接口留着比没有更危险
（删了什么、为什么删，见 [docs/移除记录.md](docs/移除记录.md)）。

也就是说：**这份软件里没有任何一处能让刀片转起来，也没有任何一处能让它停下来。**
刀片归不归固件/硬件管，得自己去看下位机那套。界面上「开始割草」只做录像和导航，
它跟刀片没有关系——不要因为按了它就去碰刀盘。

## 三、它自己停不下来

固件**有**指令丢失保护（`balance_task.c` 里超时归零速度），但它门控在
`SysVal.SecurityLevel == 0`，而 `sys.c:41` 把默认值写成了 **1**，全固件没有第二处赋值。
**这条保护永远不生效。**

所以只要上位机停发速度指令，车就会**按最后那个速度一直跑下去**。可能触发的场景：
界面卡死、Nav2 崩了、WiFi/SSH 断线、网线被踢掉、树莓派掉电、USB 转串口接触不良。

`bridge/` 里的 `chassis_bridge` 自带 0.5 秒看门狗兜这个底（停发就补一帧零速度），
但是：

- 桥进程本身崩了、被 kill 了，兜底就没了；
- 树莓派掉电或重启，兜底也没了；
- 串口线松了，帧发不出去，一样没用。

**兜底是软件兜底，不是安全回路。** 必须在硬件上另装一个**物理急停开关**，切断电机动力，
而不是指望这份代码。

## 四、上电头 10 秒和急停

- **上电后 10 秒内 STM32 丢弃所有下行指令**（`CONTROL_DELAY`）。这期间怎么推摇杆车都不动，
  不是坏了。详见 [docs/协议.md](docs/协议.md) 第三节。
- **急停是软件锁存的**，只能靠 `CMD=8` 或复位解。而且它锁的是 STM32 里的变量，
  **掉电才清**——树莓派那边重启不影响它。界面主页面有「解除急停」按钮。
- 软件急停在任何意义上都不能替代物理急停。

## 五、电池与电气

锂电池、电机驱动、大电流回路都在车上。**动手接线之前先断电池**，别带电插拔动力线。
电压采样值只是给界面看的，不是保护电路。

## 六、场地与合规

- 作业场地内**不能有人、有宠物、有小孩**，跑之前先清场。
- 操作者要保持能随时看到车、随时能切断动力的距离，不要远程盲跑。
- 遵守当地关于噪声、无线电（WiFi/蓝牙/雷达）、电池和自动机械的法规。
- 雷达是激光产品，别拿眼睛直视发射口。

## 七、现在已知没做完的

`README.md` 的「已知没做完的」那一节列着当前的窟窿（自动回充是空壳、建图页是占位符等）。
**那份清单是本文档的一部分**，上机之前一起看。

## 八、没有担保

本工程按 **MIT 协议**提供，见 [LICENSE](LICENSE)，**不附带任何担保**。

作者不对任何人身伤害、财产损失、数据丢失、作业损失、第三方索赔负责。你用这份代码，
风险自担；**改过的部分由改的人自己负责**。
